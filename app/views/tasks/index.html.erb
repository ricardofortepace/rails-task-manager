<h1>My Tasks</h1>

<div>
  <%@tasks.each do |task|%>
  <input type="checkbox"<%= task.completed == true ? "checked" : "unchecked" %>/><%= link_to task.title, task_path(task)%>
  <%end%>
</div>

<p><%= link_to "+ Add a New Task", new_task_path %></p>
